mutation VendorApplicationApprove($id: ID!) {
  vendorApplicationApprove(id: $id) {
    success
    vendorApplication {
      id
      status
      reviewedAt
    }
    errors {
      field
      message
      code
    }
  }
}
