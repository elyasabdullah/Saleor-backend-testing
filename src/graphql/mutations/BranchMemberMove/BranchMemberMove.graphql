mutation BranchMemberMove($input: BranchMemberMoveInput!) {
  branchMemberMove(input: $input) {
    success
    targetBranchId
    userIds
    errors {
      field
      message
      code
    }
  }
}
