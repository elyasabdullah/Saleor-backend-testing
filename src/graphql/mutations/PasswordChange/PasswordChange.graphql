mutation PasswordChange($input: PasswordChangeInput!, $newPassword: String!, $oldPassword: String) {
  passwordChange(input: $input, newPassword: $newPassword, oldPassword: $oldPassword) {
    user {
      id
      email
    }
    errors {
      field
      message
      code
    }
  }
}
